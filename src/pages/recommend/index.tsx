import ContentRecommend from "@/contents/recommend";
import MainLayouts from "@/layouts/main";

const PagesRecommend: React.FC = () => {
  return (
    <MainLayouts transparent fullWidth>
      <ContentRecommend />
    </MainLayouts>
  );
};

export default PagesRecommend;
